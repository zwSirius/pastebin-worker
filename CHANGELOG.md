# Changelog

## 2026-09-12

### 加密分享（替代原网页端"客户端加密"入口）

- 上传时可开启"加密分享"并设置 4-8 位分享密钥。密钥保存在服务端（与管理密码同级的保护级别），内容本身明文存储，密码仅用于限制访问。
- 受保护粘贴的访问控制：
  - 原始内容与 `/u/` 跳转需要 `X-PB-Share-Passwd` 请求头，缺失或错误返回 403；
  - 浏览器直接打开（原始链接、`/d/`、`/a/`）时返回内置密钥输入页，输对密钥后按内容类型渲染：markdown 接管 `/a/` 渲染文档、HTML 在沙箱 iframe 中渲染、图片全屏显示、音频/视频内嵌播放、其他文本直接展示、二进制自动下载；
  - 受保护内容的所有响应均为 `Cache-Control: no-store`，防止缓存绕过密钥校验。
- 未加密的 markdown 粘贴：浏览器打开原始链接会 302 到 `/a/` 渲染页（与分享 `/a/` 链接等效）；`?a`、`?mime=`、路径带扩展名/文件名或非浏览器客户端仍返回源码。受保护粘贴不做此重定向（密钥页解锁后已等效，避免重复输密钥）。
- 网页界面：
  - 设置面板开关更名为"加密分享"，勾选后出现分享密钥输入框（与开关同行）；
  - 上传结果面板按内容类型给出客户链接：markdown 显示"Markdown 链接"（`/a/`），HTML 显示"网页链接"（原始链接），文件上传按扩展名自动识别（`.md`/`.html` 等）；
  - 展示页为 HTML 粘贴提供"源码/渲染视图"切换；
  - 首页说明更新为统一规则：markdown 发 `/a/` 链接，其余一切发原始链接。
- CLI：`pb post/update/get` 新增 `-P/--share-passwd`（get 收到 403 时提示），bash/zsh/fish 补全同步。
- API：新增 `share-passwd` 表单字段与 `X-PB-Share-Passwd` 请求头；`/m/` 元数据新增 `passwordProtected` 字段。详见 `doc/api.md`、`doc/curl.md`、`doc/skill.md`。
- 兼容性：原"客户端加密"（随机密钥 + `#` 片段链接）在 CLI（`-E`）与 API（`encryption-scheme` 字段）中继续可用，展示页保留其解密路径；受保护粘贴的管理密码更新流程不变（`PUT` 不带 `share-passwd` 时保留原密钥）。
- 杂项：prettier 配置增加 `endOfLine: "auto"`，Windows CRLF 工作区不再全量报格式差异；测试覆盖密钥门禁、密钥页与解锁渲染流程（共 162 项）。
